export const Loading = () => {

   return (
      <div className="h-screen w-full bg-loading fixed flex items-center justify-center bg-no-repeat bg-cover">
         <h1 className="text-3xl animate-pulse">Carregando...</h1>
      </div>
   )
}